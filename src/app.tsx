import React, { FC } from 'react';
import { BrowserRouter as Router, Route, Link } from 'react-router-dom';

const App: FC = () => {
    return (
        <Router>
            <>Hello</>
        </Router>
    );
};

export default App;
